# The first configuration after some Linux distro installation

In order to perform that, download the ***default_program.sh*** script, open the terminal and type:

```
sudo chmod +x default_program.sh
./default_program.sh
```